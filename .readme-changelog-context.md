# README 변경이력 컨텍스트

이 파일은 gpt-5.3이 README.md 변경이력 정리 작업 시 참고하는 파일입니다.

## 2026-02-14 삭제된 변경이력 내용

### HTML 수정 사항
- `main.html` 로고 `<p>` → `<h1>` 변경 (헤더 공통화)
- `main.html` 본문 heading 계층 조정 (h1→h2, h2→h3, h3→h4)
- `sub.html` `<aside>`에서 중복 `role="complementary"` 제거
- `sub.html` 본문 `<article>` → `<section>` 변경 (독립 콘텐츠 아님)
- `main.html` 카드 `<article>` → `<div>` 변경 (UI 컴포넌트)
- `sub.html` `<img>`에 `width`/`height` 추가 (CLS 방지)

### JS 모듈 정리 (제거)
- **web-vitals.js**: Git conflict 미해결 + Vercel 분석 전송 등 템플릿에 과도한 기능
- **lazyLoad.js**: HTML `loading="lazy"` 네이티브 속성으로 충분, JS 중복
- **theme.js**: HTML에 `#darkModeToggle` 버튼 없어서 작동 안 함
- **scroll-anim.js**: HTML에 `.anim-on-scroll` 클래스 사용처 없음

### 핵심 원칙
**"심플하고 가볍게 / 과도한 기능 추가 금지 / 커스터마이징 여지 남기기"**

---

이 내용은 Git 커밋 `fe0fdc5`에서 README.md 변경이력 섹션으로 삭제되었으며,
향후 유사한 변경 시 참고할 수 있도록 여기에 보관합니다.
